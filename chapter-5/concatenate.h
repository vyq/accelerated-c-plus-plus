#ifndef CONCATENATE
#define CONCATENATE

#include <string>
#include <vector>

std::vector<std::string> ConcatenateVertically(
  const std::vector<std::string>&,
  const std::vector<std::string>&
);

std::vector<std::string> ConcatenateHorizontally(
  const std::vector<std::string>&,
  const std::vector<std::string>&
);

#endif
