#ifndef MEDIAN
#define MEDIAN

#include <vector>
#include "student.h"

double ComputeMedian(std::vector<double>);
double ComputeMedian(const std::vector<Student>&);
double ComputeOptimisticMedian(const std::vector<Student>&);

#endif
